<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medical System - Medication Management</title>
    <meta name="csrf-token" content="{{ csrfToken }}">
    <meta name="user-id" content="{{ userId }}">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="css/style.css" rel="stylesheet">
    <script>
        tailwind.config = {
            darkMode: 'class'
        }
    </script>
    <style>
        @keyframes slide-in {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        @keyframes fade-out {
            from { opacity: 1; }
            to { opacity: 0; }
        }
        .animate-slide-in { animation: slide-in 0.3s ease-out; }
        .animate-fade-out { animation: fade-out 0.3s ease-out; }
    </style>
</head>
<body class="bg-gray-100 dark:bg-gray-900 min-h-screen">
    <header class="bg-blue-600 dark:bg-blue-800 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-2xl font-bold">🏥 Medical System</h1>
                    <p class="text-sm opacity-90">Welcome, {{ username }} ({{ role }})</p>
                </div>
                <div class="flex gap-3">
                    <button id="settingsBtn" class="px-4 py-2 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-lg transition">
                        ⚙️ Settings
                    </button>
                    <a href="logout.php" class="px-4 py-2 bg-red-500 hover:bg-red-600 rounded-lg transition">
                        🚪 Logout
                    </a>
                </div>
            </div>
        </div>
    </header>

    <nav class="bg-white dark:bg-gray-800 shadow-md">
        <div class="container mx-auto px-4">
            <div class="flex gap-4 py-3">
                <a href="index.php" class="px-4 py-2 bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 transition">
                    👤 Patients
                </a>
                <a href="medication.php" class="px-4 py-2 bg-blue-600 text-white rounded-lg font-semibold">
                    💊 Medications
                </a>
                <a href="record.php" class="px-4 py-2 bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 transition">
                    📋 All Records
                </a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8 max-w-7xl">
        <!-- Medication Entry Form -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-6 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-6 border-b-2 border-green-500 pb-2">
                💊 Medication Management
            </h2>
            <form id="medForm" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <input type="hidden" id="medicationRef" name="Medication_Rec_Ref">
                
                <!-- Patient search with autocomplete dropdown -->
                <div>
                    <label for="patientSearch" 
                           class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Patient * (Search by name, surname, or patient ID card)
                    </label>
                    <input type="text" 
                           id="patientSearch" 
                           placeholder="Type to search patients..." 
                           class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 
                                  rounded-lg focus:ring-2 focus:ring-green-500 
                                  bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                    <input type="hidden" id="selectedPatientId" name="Patient_ID" required>
                    <!-- Dropdown for patient search results -->
                    <div id="patientDropdown" 
                         class="absolute z-10 w-full bg-white dark:bg-gray-800 border border-gray-300 
                                dark:border-gray-600 rounded-lg shadow-lg mt-1 max-h-60 overflow-y-auto hidden">
                    </div>
                </div>
                
                <div>
                    <label for="medicationSelect" 
                           class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Medication * 
                        <span id="medLoadingIndicator" 
                              class="text-blue-500 text-xs" 
                              style="display:none;">Loading...</span>
                    </label>
                    <select id="medicationSelect" 
                            name="Medication_Name" 
                            required 
                            class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 
                                   rounded-lg focus:ring-2 focus:ring-green-500 
                                   bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                        <option value="">Loading medications...</option>
                    </select>
                </div>
                
                <div>
                    <label for="systemDate" 
                           class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Date *
                    </label>
                    <input type="date" 
                           id="systemDate" 
                           name="System_Date" 
                           required 
                           class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 
                                  rounded-lg focus:ring-2 focus:ring-green-500 
                                  bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                </div>
                
                <div>
                    <label for="remarks" 
                           class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Remarks
                    </label>
                    <input type="text" 
                           id="remarks" 
                           name="Remarks" 
                           class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 
                                  rounded-lg focus:ring-2 focus:ring-green-500 
                                  bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                           placeholder="Enter medication remarks">
                </div>
                
                <div class="col-span-full flex gap-3">
                    <button type="submit" 
                            class="flex-1 bg-green-600 hover:bg-green-700 text-white 
                                   font-semibold py-2 px-4 rounded-lg transition">
                        💾 Save Medication
                    </button>
                    <button type="button" 
                            id="medReset" 
                            class="flex-1 bg-gray-500 hover:bg-gray-600 text-white 
                                   font-semibold py-2 px-4 rounded-lg transition">
                        🔄 Reset Form
                    </button>
                </div>
            </form>
        </div>

        <!-- Medication List Table -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden">
            <div class="p-4 border-b border-gray-200 dark:border-gray-700">
                <h3 class="text-xl font-bold text-gray-800 dark:text-white">📅 Recent Medications (DESC)</h3>
                <p class="text-sm text-gray-600 dark:text-gray-400 mt-1">Showing most recently prescribed medications first</p>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-50 dark:bg-gray-700">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Patient ID Card</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Patient</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Medication</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Date</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Prescribed By</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Remarks</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="medicationsTableBody" class="divide-y divide-gray-200 dark:divide-gray-700">
                        <tr><td colspan="7" class="px-4 py-8 text-center text-gray-500">Loading medications...</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <footer class="bg-gray-800 dark:bg-gray-900 text-white py-4 mt-8">
        <div class="container mx-auto px-4 text-center">
            <p class="text-sm opacity-75">
                © 2025 Martina Mallia - Full Stack Development Assignment
            </p>
        </div>
    </footer>

    <div id="settingsModal" style="display: none;" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
        <div class="bg-white dark:bg-gray-800 rounded-lg p-6 w-full max-w-md">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800 dark:text-white">⚙️ Settings</h2>
                <button id="closeSettings" class="text-gray-500 hover:text-gray-700 dark:text-gray-400 dark:hover:text-gray-200 text-2xl">&times;</button>
            </div>
            
            <div class="space-y-4">
                <div class="flex items-center justify-between">
                    <label class="text-gray-700 dark:text-gray-300 font-medium">🌙 Dark Mode</label>
                    <label class="relative inline-flex items-center cursor-pointer">
                        <input type="checkbox" id="darkModeToggle" class="sr-only peer">
                        <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 dark:peer-focus:ring-blue-800 rounded-full peer dark:bg-gray-700 peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all dark:border-gray-600 peer-checked:bg-blue-600"></div>
                    </label>
                </div>
                
                <div class="flex items-center justify-between">
                    <label class="text-gray-700 dark:text-gray-300 font-medium">📊 Table Limit</label>
                    <select id="tableLimitSelect" class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500">
                        <option value="5">5</option>
                        <option value="10">10</option>
                        <option value="25">25</option>
                        <option value="50">50</option>
                        <option value="100">100</option>
                    </select>
                </div>
            </div>
            
            <div class="flex gap-3 mt-6">
                <button id="saveSettings" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg">
                    💾 Save
                </button>
                <button id="resetSettings" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white py-2 rounded-lg">
                    🔄 Reset
                </button>
            </div>
        </div>
    </div>

    <script src="js/common.js?v=10.5"></script>
    <script src="js/medication.js?v=3.0"></script>
</body>
</html>