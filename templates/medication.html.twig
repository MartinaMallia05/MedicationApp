<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Medical System - Medication Management</title>
    <meta name="csrf-token" content="{{ csrfToken }}">
    <meta name="user-id" content="{{ userId }}">
    <!-- TODO: Replace CDN with production Tailwind setup via npm/PostCSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="css/style.css" rel="stylesheet">
    <script>
        tailwind.config = {
            darkMode: 'class'
        }
    </script>
    <style>
        @keyframes slide-in {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        @keyframes fade-out {
            from { opacity: 1; }
            to { opacity: 0; }
        }
        .animate-slide-in { animation: slide-in 0.3s ease-out; }
        .animate-fade-out { animation: fade-out 0.3s ease-out; }
    </style>
</head>
<body class="bg-gray-100 dark:bg-gray-900 min-h-screen">
    <!-- Header -->
    <header class="bg-blue-600 dark:bg-blue-800 text-white shadow-lg">
        <div class="container mx-auto px-4 py-4">
            <div class="flex justify-between items-center">
                <div>
                    <h1 class="text-2xl font-bold">🏥 Medical System</h1>
                    <p class="text-sm opacity-90">Welcome, {{ username }} ({{ role }})</p>
                </div>
                <div class="flex gap-3">
                    <button id="settingsBtn" class="px-4 py-2 bg-white bg-opacity-20 hover:bg-opacity-30 rounded-lg transition">
                        ⚙️ Settings
                    </button>
                    <a href="logout.php" class="px-4 py-2 bg-red-500 hover:bg-red-600 rounded-lg transition">
                        🚪 Logout
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="bg-white dark:bg-gray-800 shadow-md">
        <div class="container mx-auto px-4">
            <div class="flex gap-4 py-3">
                <a href="index.php" class="px-4 py-2 bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 transition">
                    👤 Patients
                </a>
                <a href="medication.php" class="px-4 py-2 bg-blue-600 text-white rounded-lg font-semibold">
                    💊 Medications
                </a>
                <a href="record.php" class="px-4 py-2 bg-gray-200 dark:bg-gray-700 text-gray-700 dark:text-gray-300 rounded-lg hover:bg-gray-300 dark:hover:bg-gray-600 transition">
                    📋 All Records
                </a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto px-4 py-8 max-w-7xl">
        <!-- Medication Form -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg p-6 mb-8">
            <h2 class="text-2xl font-bold text-gray-800 dark:text-white mb-6 border-b-2 border-green-500 pb-2">
                💊 Medication Management
            </h2>
            <form id="medForm" class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <input type="hidden" id="medicationRef" name="Medication_Rec_Ref">
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Patient *</label>
                    <select id="patientSelect" name="Patient_ID" required 
                        class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                        <option value="">Select Patient</option>
                    </select>
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">
                        Medication * <span id="medLoadingIndicator" class="text-blue-500 text-xs" style="display:none;">Loading...</span>
                    </label>
                    <select id="medicationSelect" name="Medication_Name" required 
                        class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                        <option value="">Loading medications...</option>
                    </select>
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Date *</label>
                    <input type="date" id="systemDate" name="System_Date" required 
                        class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white">
                </div>
                
                <div>
                    <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-1">Remarks *</label>
                    <input type="text" id="remarks" name="Remarks" required 
                        class="w-full px-3 py-2 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-green-500 bg-white dark:bg-gray-700 text-gray-900 dark:text-white"
                        placeholder="Enter medication remarks">
                </div>
                
                <div class="col-span-full flex gap-3">
                    <button type="submit" class="flex-1 bg-green-600 hover:bg-green-700 text-white font-semibold py-2 px-4 rounded-lg transition">
                        💾 Save Medication
                    </button>
                    <button type="button" id="medReset" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-semibold py-2 px-4 rounded-lg transition">
                        🔄 Reset Form
                    </button>
                </div>
            </form>
        </div>

        <!-- Recent Medications Table -->
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-lg overflow-hidden">
            <div class="p-4 border-b border-gray-200 dark:border-gray-700">
                <h3 class="text-xl font-bold text-gray-800 dark:text-white">Recent Medications</h3>
            </div>
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead class="bg-gray-50 dark:bg-gray-700">
                        <tr>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Patient</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Medication</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Date</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Remarks</th>
                            <th class="px-4 py-3 text-left text-xs font-medium text-gray-700 dark:text-gray-300 uppercase">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="medicationsTableBody" class="divide-y divide-gray-200 dark:divide-gray-700">
                        <tr><td colspan="5" class="px-4 py-8 text-center text-gray-500">Loading medications...</td></tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Settings Modal -->
    <div id="settingsModal" style="display: none;" class="fixed inset-0 bg-black bg-opacity-50 z-50 flex items-center justify-center p-4">
        <div class="bg-white dark:bg-gray-800 rounded-lg shadow-2xl max-w-md w-full p-6">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-gray-800 dark:text-white">⚙️ Settings</h2>
                <button class="close text-3xl text-gray-500 hover:text-gray-700 dark:hover:text-gray-300">&times;</button>
            </div>
            
            <div class="space-y-4">
                <div class="flex items-center justify-between p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                    <label for="darkModeToggle" class="text-gray-700 dark:text-gray-300">🌙 Dark Mode</label>
                    <input type="checkbox" id="darkModeToggle" class="w-12 h-6">
                </div>
                
                <div class="flex items-center justify-between p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                    <label for="tableLimitSelect" class="text-gray-700 dark:text-gray-300">📊 Rows per page</label>
                    <select id="tableLimitSelect" class="px-3 py-1 border border-gray-300 dark:border-gray-600 rounded bg-white dark:bg-gray-800 text-gray-900 dark:text-white">
                        <option value="10">10</option>
                        <option value="25">25</option>
                        <option value="50">50</option>
                        <option value="100">100</option>
                    </select>
                </div>
            </div>
            
            <div class="flex gap-3 mt-6">
                <button id="saveSettings" class="flex-1 bg-blue-600 hover:bg-blue-700 text-white py-2 rounded-lg">
                    Save
                </button>
                <button id="resetSettings" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white py-2 rounded-lg">
                    Reset
                </button>
            </div>
        </div>
    </div>

    <!-- JavaScript Files - LOAD IN ORDER -->
    <script src="js/common.js"></script>
    <script src="js/medication.js"></script>
</body>
</html>